export default function ParentHomePage() {
  return (
    <section className="card" style={{ padding: "1rem" }}>
      <h1 style={{ marginTop: 0 }}>Parent Dashboard</h1>
      <p style={{ color: "var(--ink-subtle)" }}>
        Parent role portal is active with consent history and notification visibility.
      </p>
    </section>
  );
}
