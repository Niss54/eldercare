export default function DoctorHomePage() {
  return (
    <section className="card" style={{ padding: "1rem" }}>
      <h1 style={{ marginTop: 0 }}>Doctor Dashboard</h1>
      <p style={{ color: "var(--ink-subtle)" }}>
        Doctor role portal is active with health record and medication monitoring access.
      </p>
    </section>
  );
}
